package model;

/**
 * Created by robin on 16-3-17.
 */
public class AlgorithmPseudo {

    public void boggleSolve() {
         /*
         select first character
         while (character has neighbourcharacter) {  //Voor alle buren dus noord,zuid,zuidwest etc check of je er een woord mee kan maken.
            // (deze moet nog verder uitgewerkt worden)
            woord = character + neighbourcharacter;
            if (woordenlijst.contains(woord)){ // Onze letters staan in de woordenlijst -> check of dit een geheel woord is of een deel van het woord.
                move candidate from candidates to solution
                if (woord = heel woord){
                    System.out.print("Gevonden woord: " + woord);
                    //woord kan ook nog steeds een deel van een langer woord zijn dus ga door.
                    boggleSolve();
                } else {
                    // We gaan door naar het volgende character
                    boggleSolve();  //recursie
                }
            } //if (woordenlijst.contains(woord)
            //geen succes gehad met de volgende characters -> dus weggooien begin character en door naar de volgende
            woord = "";
            select next character
        } //while

    } //bogglesolve
    */
    }
}
